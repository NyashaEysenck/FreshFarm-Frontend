const express=require("express"),cors=require("cors"),bodyParser=require("body-parser"),connectDB=require("./config/db"),submissionRoutes=require("./routes/submissionRoutes");require("dotenv").config();const app=express();connectDB(),app.use(cors()),app.use(bodyParser.json()),app.use("/api/submissions",submissionRoutes),app.use(((s,e,r,o)=>{console.error(s.stack),r.status(500).json({error:"Something went wrong!"})}));const PORT=process.env.PORT||5e3;app.listen(PORT,(()=>console.log(`Server running on port ${PORT}`)));